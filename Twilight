<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Twilight Net - Coming Soon</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            background: linear-gradient(135deg, #0a0a0a 0%, #1a1a2e 50%, #16213e 100%);
            min-height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            font-family: 'Courier New', monospace;
            overflow: hidden;
            position: relative;
        }

        /* Animated background */
        body::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background-image: 
                repeating-linear-gradient(0deg, transparent, transparent 2px, rgba(0, 255, 136, 0.03) 2px, rgba(0, 255, 136, 0.03) 4px),
                repeating-linear-gradient(90deg, transparent, transparent 2px, rgba(0, 255, 136, 0.03) 2px, rgba(0, 255, 136, 0.03) 4px);
            animation: gridMove 20s linear infinite;
        }

        @keyframes gridMove {
            0% { transform: translate(0, 0); }
            100% { transform: translate(50px, 50px); }
        }

        .container {
            text-align: center;
            z-index: 10;
            padding: 40px;
        }

        .title {
            font-size: 3.5em;
            color: #00ff88;
            text-shadow: 0 0 20px rgba(0, 255, 136, 0.5),
                         0 0 40px rgba(0, 255, 136, 0.3);
            margin-bottom: 20px;
            letter-spacing: 8px;
            animation: glitch 3s infinite;
        }

        @keyframes glitch {
            0%, 90%, 100% {
                transform: translate(0);
            }
            92% {
                transform: translate(-2px, 2px);
            }
            94% {
                transform: translate(2px, -2px);
            }
        }

        .subtitle {
            font-size: 1.8em;
            color: #4ecdc4;
            text-shadow: 0 0 10px rgba(78, 205, 196, 0.5);
            margin-bottom: 50px;
            letter-spacing: 4px;
            animation: fadeInOut 2s ease-in-out infinite;
        }

        @keyframes fadeInOut {
            0%, 100% { opacity: 0.7; }
            50% { opacity: 1; }
        }

        /* Coder character */
        .coder-scene {
            position: relative;
            width: 300px;
            height: 250px;
            margin: 0 auto 40px;
        }

        .chair {
            position: absolute;
            bottom: 20px;
            left: 50%;
            transform: translateX(-50%);
            width: 120px;
            height: 100px;
            animation: chairSwivel 4s ease-in-out infinite;
        }

        @keyframes chairSwivel {
            0%, 100% { transform: translateX(-50%) rotate(0deg); }
            25% { transform: translateX(-55%) rotate(-3deg); }
            75% { transform: translateX(-45%) rotate(3deg); }
        }

        .chair-seat {
            width: 80px;
            height: 15px;
            background: #333;
            border-radius: 50% 50% 0 0;
            margin: 0 auto;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.5);
        }

        .chair-back {
            width: 60px;
            height: 40px;
            background: #2a2a2a;
            border-radius: 8px 8px 0 0;
            margin: 0 auto;
            position: relative;
            top: -25px;
        }

        .chair-pole {
            width: 8px;
            height: 30px;
            background: #444;
            margin: 0 auto;
        }

        .chair-base {
            width: 70px;
            height: 5px;
            background: #333;
            margin: 0 auto;
            border-radius: 50%;
        }

        .coder {
            position: absolute;
            bottom: 60px;
            left: 50%;
            transform: translateX(-50%);
            animation: typing 2s ease-in-out infinite;
        }

        @keyframes typing {
            0%, 100% { transform: translateX(-50%) translateY(0); }
            50% { transform: translateX(-50%) translateY(-3px); }
        }

        .head {
            width: 40px;
            height: 40px;
            background: #ffdbac;
            border-radius: 50%;
            margin: 0 auto 5px;
            position: relative;
        }

        .hair {
            width: 35px;
            height: 20px;
            background: #4a2c2a;
            border-radius: 50% 50% 0 0;
            position: absolute;
            top: -5px;
            left: 2.5px;
        }

        .eyes {
            display: flex;
            gap: 12px;
            justify-content: center;
            margin-top: 15px;
        }

        .eye {
            width: 4px;
            height: 4px;
            background: #000;
            border-radius: 50%;
        }

        .body {
            width: 50px;
            height: 45px;
            background: #1e3a5f;
            margin: 0 auto;
            border-radius: 8px 8px 0 0;
            position: relative;
        }

        .arm {
            width: 25px;
            height: 8px;
            background: #1e3a5f;
            position: absolute;
        }

        .arm-left {
            top: 10px;
            left: -20px;
            transform: rotate(-45deg);
            border-radius: 4px 0 0 4px;
        }

        .arm-right {
            top: 10px;
            right: -20px;
            transform: rotate(45deg);
            border-radius: 0 4px 4px 0;
        }

        .coffee {
            position: absolute;
            right: -35px;
            top: 20px;
            width: 20px;
            height: 25px;
            background: #8b4513;
            border-radius: 0 0 8px 8px;
            animation: sip 3s ease-in-out infinite;
        }

        @keyframes sip {
            0%, 100% { transform: translateY(0); }
            50% { transform: translateY(-5px); }
        }

        .coffee::before {
            content: '';
            position: absolute;
            top: -3px;
            left: 50%;
            transform: translateX(-50%);
            width: 2px;
            height: 12px;
            background: #666;
        }

        .coffee::after {
            content: '';
            position: absolute;
            top: 2px;
            left: 2px;
            width: 16px;
            height: 8px;
            background: #6b3410;
            border-radius: 50%;
        }

        .computer {
            position: absolute;
            bottom: 115px;
            left: 50%;
            transform: translateX(-50%);
            width: 100px;
            height: 70px;
            background: #1a1a1a;
            border-radius: 4px;
            box-shadow: 0 0 20px rgba(0, 255, 136, 0.3);
        }

        .screen {
            width: 90px;
            height: 55px;
            background: linear-gradient(135deg, #0a4d3c 0%, #083d2f 100%);
            margin: 5px auto;
            border-radius: 2px;
            position: relative;
            overflow: hidden;
        }

        .code-line {
            width: 80%;
            height: 2px;
            background: #00ff88;
            margin: 3px auto;
            opacity: 0.8;
            animation: codeBlink 1.5s ease-in-out infinite;
        }

        @keyframes codeBlink {
            0%, 100% { opacity: 0.4; }
            50% { opacity: 1; }
        }

        .code-line:nth-child(2) { width: 60%; animation-delay: 0.2s; }
        .code-line:nth-child(3) { width: 70%; animation-delay: 0.4s; }
        .code-line:nth-child(4) { width: 50%; animation-delay: 0.6s; }

        .loading {
            margin-top: 30px;
            color: #00ff88;
            font-size: 1.2em;
            letter-spacing: 2px;
        }

        .dots {
            display: inline-block;
        }

        .dots span {
            animation: dotBlink 1.5s infinite;
        }

        .dots span:nth-child(2) { animation-delay: 0.3s; }
        .dots span:nth-child(3) { animation-delay: 0.6s; }

        @keyframes dotBlink {
            0%, 60%, 100% { opacity: 0; }
            30% { opacity: 1; }
        }

        @media (max-width: 768px) {
            .title {
                font-size: 2em;
                letter-spacing: 4px;
            }
            .subtitle {
                font-size: 1.2em;
                letter-spacing: 2px;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <h1 class="title">WELCOME TO TWILIGHT NET</h1>
        <h2 class="subtitle">WE ARE COMING SOON</h2>
        
        <div class="coder-scene">
            <div class="computer">
                <div class="screen">
                    <div class="code-line"></div>
                    <div class="code-line"></div>
                    <div class="code-line"></div>
                    <div class="code-line"></div>
                </div>
            </div>
            
            <div class="coder">
                <div class="head">
                    <div class="hair"></div>
                    <div class="eyes">
                        <div class="eye"></div>
                        <div class="eye"></div>
                    </div>
                </div>
                <div class="body">
                    <div class="arm arm-left"></div>
                    <div class="arm arm-right"></div>
                    <div class="coffee"></div>
                </div>
            </div>
            
            <div class="chair">
                <div class="chair-back"></div>
                <div class="chair-seat"></div>
                <div class="chair-pole"></div>
                <div class="chair-base"></div>
            </div>
        </div>
        
        <div class="loading">
            INITIALIZING<span class="dots"><span>.</span><span>.</span><span>.</span></span>
        </div>
    </div>
</body>
</html>